{
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "docs:deploy": "git subtree push --prefix .vitepress/dist origin gh-pages"
  },
  "devDependencies": {
    "vue": "^3.5.16"
  },
  "dependencies": {
    "vitepress": "^1.6.3"
  }
}